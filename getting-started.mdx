---
title: "Getting Started: Install and Use Bits UI in Svelte 5"
description: "Install Bits UI and build your first accessible headless component. Covers basic usage, Tailwind styling, data attribute selectors, and TypeScript support."
---

Bits UI is a collection of headless component primitives for Svelte 5 that prioritizes developer experience, accessibility, and flexibility. This guide walks you through installing Bits UI and using it in your Svelte application.

## Installation

Install Bits UI using your preferred package manager.

<CodeGroup>

```bash npm
npm install bits-ui
```

```bash pnpm
pnpm add bits-ui
```

```bash yarn
yarn add bits-ui
```

```bash bun
bun add bits-ui
```

</CodeGroup>

<Note>
  Bits UI requires Svelte 5 (`^5.33.0`). If you're on Svelte 4, see the [v0 documentation](https://v0.bits-ui.com) or follow the [migration guide](/migration-guide).
</Note>

## Basic usage

After installation, import and use Bits UI components in your Svelte files. Here's a simple example using the [Accordion](/components/accordion) component:

```svelte
<script lang="ts">
  import { Accordion } from "bits-ui";
</script>

<Accordion.Root type="single">
  <Accordion.Item value="item-1">
    <Accordion.Header>
      <Accordion.Trigger>Item 1 Title</Accordion.Trigger>
    </Accordion.Header>
    <Accordion.Content>
      This is the collapsible content for this section.
    </Accordion.Content>
  </Accordion.Item>
  <Accordion.Item value="item-2">
    <Accordion.Header>
      <Accordion.Trigger>Item 2 Title</Accordion.Trigger>
    </Accordion.Header>
    <Accordion.Content>
      This is the collapsible content for this section.
    </Accordion.Content>
  </Accordion.Item>
</Accordion.Root>
```

## Adding styles

Bits UI components are headless by design — they ship with minimal styling. Each component that renders an HTML element exposes a `class` prop and a `style` prop for full control over appearance.

### Styling with Tailwind CSS or UnoCSS

If you're using a CSS framework like Tailwind CSS or UnoCSS, pass classes directly to the component:

```svelte
<script lang="ts">
  import { Accordion } from "bits-ui";
</script>

<Accordion.Root class="mx-auto w-full max-w-md">
  <Accordion.Item class="mb-2 rounded-md border border-gray-200">
    <Accordion.Header class="bg-gray-50 transition-colors hover:bg-gray-100">
      <Accordion.Trigger
        class="flex w-full items-center justify-between p-4 text-left font-medium"
      >
        <span>Tailwind-styled Accordion</span>
        <svg
          class="h-5 w-5 transform transition-transform"
          xmlns="http://www.w3.org/2000/svg"
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M19 9l-7 7-7-7"
          />
        </svg>
      </Accordion.Trigger>
    </Accordion.Header>
    <Accordion.Content class="p-4 text-gray-700">
      This accordion is styled using Tailwind CSS classes.
    </Accordion.Content>
  </Accordion.Item>
</Accordion.Root>
```

### Styling with data attributes

Each Bits UI component applies specific data attributes to the underlying HTML elements. Use these attributes to target components in your global styles:

<Steps>
  <Step title="Check the API reference">
    Look up each component's API reference to find its data attributes.
  </Step>
  <Step title="Write global CSS rules">
    Target those data attributes in your stylesheet:

    ```css title="app.css"
    [data-button-root] {
      height: 3rem;
      width: 100%;
      background-color: #3182ce;
      color: white;
      border-radius: 0.375rem;
      padding: 0.5rem 1rem;
      font-weight: 500;
    }

    [data-button-root]:hover {
      background-color: #2c5282;
    }
    ```
  </Step>
  <Step title="Import the stylesheet in your layout">
    ```svelte title="+layout.svelte"
    <script lang="ts">
      import { Button } from "bits-ui";
      import "../app.css";
    </script>

    <Button.Root>Click me</Button.Root>
    ```

    Every `Button.Root` component will now have these styles applied automatically.
  </Step>
</Steps>

## TypeScript support

Bits UI is built with TypeScript and provides comprehensive type definitions. You get full type checking and autocompletion when using TypeScript:

```svelte
<script lang="ts">
  import { Accordion } from "bits-ui";

  // TypeScript will validate these props
  const accordionMultipleProps: Accordion.RootProps = {
    type: "multiple",
    value: ["item-1"], // type error if value is not an array
  };

  const accordionSingleProps: Accordion.RootProps = {
    type: "single",
    value: "item-1", // type error if value is an array
  };
</script>
```

## Next steps

<CardGroup cols={2}>
  <Card title="Browse components" icon="grid-2" href="/components/accordion">
    Explore all available headless component primitives.
  </Card>
  <Card title="Child snippet" icon="code" href="/child-snippet">
    Learn render delegation for maximum flexibility and customization.
  </Card>
  <Card title="State management" icon="sliders" href="/state-management">
    Understand how Bits UI handles state and how to take full control.
  </Card>
  <Card title="Styling guide" icon="paintbrush" href="/styling">
    Deep dive into all styling approaches supported by Bits UI.
  </Card>
</CardGroup>

## Getting help

<CardGroup cols={2}>
  <Card title="GitHub Issues" icon="bug" href="https://github.com/huntabyte/bits-ui/issues">
    Report confirmed bugs on GitHub.
  </Card>
  <Card title="Discord community" icon="discord" href="https://discord.gg/fdXy3Sk8Gq">
    Chat with other developers and the Bits UI team.
  </Card>
  <Card title="GitHub Discussions" icon="comments" href="https://github.com/huntabyte/bits-ui/discussions/new">
    Ask questions and share ideas.
  </Card>
  <Card title="Feature requests" icon="lightbulb" href="https://github.com/huntabyte/bits-ui/discussions/new?category=feature-requests-ideas">
    Propose new features for discussion.
  </Card>
</CardGroup>
